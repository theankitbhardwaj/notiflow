spring:
  application:
    name: notification
  datasource:
    url: jdbc:postgresql://localhost:5432/notiflow?useSSL=false&serverTimezone
    username: ankit
    password: ankit
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
